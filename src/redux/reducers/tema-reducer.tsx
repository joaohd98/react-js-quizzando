import {StateInterface} from "../../components/input/input";

const initialState: { filtro: StateInterface } = {
  filtro: {}
};

export const temaReducer = (state = initialState, action) => {

  switch (action.type) {

    default:
      return state

  }

};
